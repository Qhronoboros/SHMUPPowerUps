/// <summary>
/// The Actions of Keys (KeyCodes)
/// </summary>
public enum KeyAction
{
    NONE,       // Not Assigned (Defaults to Pressed)
    PRESSED,    // Key is Pressed
    HELD,       // Key is Held
    RELEASED    // Key is Released
}
